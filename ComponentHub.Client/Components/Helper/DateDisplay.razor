@using System.Globalization
@using MudBlazor.Extensions

@if (DateTime.HasValue)
{
    @DateTime.Value.LocalDateTime.ToString(CultureInfo.CurrentCulture)
}



@code {
    [Parameter, EditorRequired]
    public DateTimeOffset? DateTime { get; set; }
}