@using System.Globalization
@using MudBlazor.Extensions

@if (DateTime.HasValue)
{
    @DateTime.Value.LocalDateTime.ToIsoDateString()
}



@code {
    [Parameter, EditorRequired]
    public DateTimeOffset? DateTime { get; set; }
}