@page "/UploadComponentTest"
@using ComponentHub.Shared.DatabaseObjects
@using Microsoft.AspNetCore.Authorization
@using ComponentHub.Client.Components.Features.RedirectHelper
@using System.Security.Claims
@inject RedirectHelper redirectHelper
@attribute [Authorize]

<MudForm></MudForm>

@code {
    //private WclComponent _model = new WclComponent();
    
    
    [CascadingParameter]
    private Task<AuthenticationState>? authenticationState { get; set; }

    protected override async Task OnInitializedAsync()
    {
        if (authenticationState is null)
        {
            redirectHelper.Redirect("/");
            return;
        }
        
        var authStat = await authenticationState;
        //_model = new WclComponent(authStat.User.FindFirst(ClaimTypes.NameIdentifier)
    }

}